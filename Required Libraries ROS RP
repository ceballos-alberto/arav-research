- Install and configure git lfs
	https://git-lfs.github.com/
	Follow the instructions under Getting Started (befire git lfs install sudo ./install)
	No need to add anything
	
	TO ADD HEAVY FILE
	- "delete" file
	- use git lfs track "filenameONLY"
	- git add .gitattributes
	- commit
	- push
	- put file
	- push
	
- Install ROS Noetic
- Install Octomap

sudo apt-get install ros-noetic-octomap ros-noetic-octomap-mapping ros-noetic-octomap-msgs ros-noetic-octomap-ros ros-noetic-octomap-rviz-plugins ros-noetic-octomap-server

- Install ros control

sudo apt-get install ros-noetic-ros-control ros-noetic-ros-controllers

- Install TensorFlow

https://www.tensorflow.org/install

pip install --upgrade pip
pip install tensorflow

- Install ompl library

sudo apt install libompl-dev

- Install FCL library
	- First install boost library
	sudo apt-get install libboost-all-dev
	- Then install libccd
	 sudo apt-get install libccd-dev 

	- Finally install the library
	(USE VERSION 0.5)
	https://github.com/flexible-collision-library/fcl/releases


 MISS PATH PLANNING PART
